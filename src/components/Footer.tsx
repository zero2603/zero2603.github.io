const Footer = () => (
    <footer className="container">
        <div className="w-full text-right py-8 border-t border-gray-800 text-sm font-light text-white">© {new Date().getFullYear()} Pham Thu (zero2603).</div>
    </footer>
);

export default Footer;